# Supabase migrations

Database changes will be added as ordered, immutable SQL migrations beginning
with AGM-010. The database must never be maintained through undocumented manual
dashboard changes.
