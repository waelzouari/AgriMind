# Deployment

Deployment assets for Mosquitto, systemd, containers, and demo recovery will be
introduced by their owning tickets. Secrets are injected at runtime and are not
stored in this directory.
